apply {
    from "${rootDir.path}/config_lib.gradle"
}

dependencies {
    compileOnly project(':fragmentation-with-androidx')
    compileOnly Deps.appcompat
}

apply from: "${rootDir.path}/config_bintray.gradle"